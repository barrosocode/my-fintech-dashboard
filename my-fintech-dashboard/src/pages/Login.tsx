// Imports
import {Typography, Grid} from "@mui/material";

// Component
const Login = () => {
    return (
        <Grid>
            <Typography>Página de Login</Typography>
        </Grid>
    );
};

// Export
export default Login;
